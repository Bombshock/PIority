# PIority

This project uses the **DOX** framework: `AGENTS.md` files are binding work contracts for their subtrees. Read the applicable DOX chain (root → nearest owning doc) before editing, and run a DOX pass to update the owning `AGENTS.md` after every meaningful change.

The root contract, project layout, local contracts, verification steps, and the Child DOX Index are imported below.

@AGENTS.md
